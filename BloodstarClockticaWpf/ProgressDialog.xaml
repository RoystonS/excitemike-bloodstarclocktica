<Window x:Class="BloodstarClockticaWpf.ProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BloodstarClockticaWpf"
        xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize" 
        Style="{StaticResource WindowStyle}" SizeToContent="WidthAndHeight"
        Title="Working">
    <StackPanel>
        <!-- title bar -->
        <Grid Height="30">
            <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding (Window.Title), RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type Window}}}"/>
        </Grid>

        <!-- progressbar view -->
        <ProgressBar x:Name="ProgressBar" 
                         Width="500"
                         Height="50" 
                         Minimum="0" 
                         Maximum="1"
                         Value="0"
                         Margin="6"
                         SmallChange="1"/>
    </StackPanel>
</Window>
